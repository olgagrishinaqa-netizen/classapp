services:
  web:
    image: cr.yandex/${REGISTRY_ID}/classapp:${IMAGE_TAG:-latest}
    environment:
      FLASK_CONFIG: config.ProdConfig
      SECRET_KEY: ${SECRET_KEY}
      DATABASE_URL: ${DATABASE_URL}
    ports:
      - "80:8000"
    restart: unless-stopped
