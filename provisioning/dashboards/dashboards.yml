apiVersion: 1

providers:
  - name: classapp-observability
    orgId: 1
    folder: ClassApp
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 10
    options:
      path: /var/lib/grafana/dashboards
